[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-deploy-policy"
version = "0.1.0"
description = "Deploy policy — P0/P1/P2 deadband classification for fleet operations"
license = {text = "MIT"}
requires-python = ">=3.8"
